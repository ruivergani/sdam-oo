using System;

namespace math_exercise
{
    class Program
    {
        static void Main(string[] args)
        {
            Math.Max(5, 10);    // highest value of x,y
            Math.Min(5, 10);    // lowest value of x and y
            Math.Sqrt(64);      // square root of x
            Math.Abs(-4.7);     // absolute (positive)
            Math.Round(9.99);   // round number
        }
    }
}
